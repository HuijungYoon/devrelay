{
  "mcpServers": {
    "redmine": {
      "command": "npx",
      "args": ["-y", "redmine-devrelay@0.3.2"],
      "env": {
        "REDMINE_URL": "${REDMINE_URL}",
        "REDMINE_API_KEY": "${REDMINE_API_KEY}",
        "REDMINE_ALLOWED_HOSTS": "${REDMINE_ALLOWED_HOSTS}"
      }
    }
  }
}
